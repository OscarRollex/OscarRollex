//program to display the first 50 natural numbers in reverse order
#include<stdio.h>
int main()
{
    int num;
    for(num=50; num>=1; num--)
    {
        printf("%d", num);
    }
    return 0;
}
